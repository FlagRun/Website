- if current_user
  %ul.nav.pull-right
    %li
      = link_to(image_tag('default/mail.png'), messages_path) if current_user.unread_messages?

    %li.dropdown
      %a.dropdown-toggle{href: '#', 'data-toggle' => 'dropdown'}
        = image_tag raw(current_user.gravatar_url size: 16   )
        = current_user.username
        %b.caret
      %ul.dropdown-menu
        %li
          = link_to 'Profile', user_path(current_user)
        %li
          =link_to 'Settings', edit_user_registration_path
        %li.divider
        %li
          = link_to '#{current_user.unread_message_count} Messages', messages_path
        %li
          %a{:href => '#'} 0 Alerts
        %li.divider
        %li= link_to 'Logout', destroy_user_session_url, :method => :delete
- else
  -#= form_tag main_app.new_user_session_path, class: 'navbar-form pull-right' do
  -#  = text_field_tag 'user[email]', nil, placeholder: 'E-Mail Address', class: 'input-small'
  -#  = password_field_tag 'user[password]', nil, placeholder: 'Password', class: 'input-small'
  -#  = check_box_tag 'user[remember_me]'
  -#  = submit_tag 'Login'
  %ul.nav.pull-right
    %li=link_to 'Login', new_user_session_path
    %li=link_to 'Register', new_user_registration_path

